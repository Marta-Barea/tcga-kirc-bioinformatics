% Preamble personalizado para portada y formato

% Paquetes adicionales
\usepackage{tikz}
\usepackage[table]{xcolor}
\usepackage{caption}
\usepackage{fancyhdr}
\usepackage{float}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{placeins}
\usepackage{fvextra}
\usepackage{array}
\usepackage{csquotes}
\usepackage{titlesec}

% Mantiene el formato APA de las referencias, pero muestra etiquetas [n]
% en la bibliografia para que coincidan con las citas numericas del texto.
\AtBeginDocument{%
  \ExecuteBibliographyOptions{labelnumber}%
  \DeclareFieldFormat{labelnumberwidth}{\mkbibbrackets{#1}}%
  \defbibenvironment{bibliography}
     {\list
         {\printtext[labelnumberwidth]{%
             \printfield{labelprefix}%
             \printfield{labelnumber}}}
         {\setlength{\labelwidth}{\labelnumberwidth}%
          \setlength{\leftmargin}{\labelwidth}%
          \setlength{\labelsep}{\biblabelsep}%
          \addtolength{\leftmargin}{\labelsep}%
          \setlength{\itemsep}{\bibitemsep}%
          \setlength{\parsep}{\bibparsep}}%
          \renewcommand*{\makelabel}[1]{\hss##1}}%
     {\endlist}%
     {\item}%
}

\raggedbottom

\DefineVerbatimEnvironment{Highlighting}{Verbatim}{%
    breaklines,
    breakanywhere,
    fontsize=\normalsize,
    commandchars=\\\{\}
}
\fvset{breaklines=true, breakanywhere=true}

\definecolor{uocblue}{RGB}{0,102,153}

% Formato de captions
\captionsetup[figure]{labelfont=bf,labelsep=period,font=small}
\captionsetup[table]{labelfont=bf,labelsep=period,font=small}
\addto\captionsspanish{\renewcommand{\tablename}{Tabla}}

% Hace que los encabezados de quinto nivel no aparezcan en linea con el texto.
\titleformat{\paragraph}[block]{\normalfont\normalsize\bfseries}{\theparagraph}{1em}{}
\titlespacing*{\paragraph}{0pt}{3.25ex plus 1ex minus .2ex}{0.8em}

\setcounter{secnumdepth}{5}
\setcounter{tocdepth}{5}

\titleformat{\subparagraph}[block]
{\normalfont\normalsize\bfseries}
{\thesubparagraph}
{1em}
{}

\titlespacing*{\subparagraph}
{0pt}
{3.25ex plus 1ex minus .2ex}
{0.8em}

% Estilo de cabeceras
\pagestyle{fancy}
\fancyhf{}

% Páginas impares
\fancyhead[RO]{\small Software para el Análisis de Datos}

% Páginas pares
\fancyhead[LE]{\small Dra.\ Marta Barea Sepúlveda}

% Número de página en el pie centrado
\fancyfoot[C]{\thepage}

% Línea superior de la cabecera
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% Aplicar también en páginas plain
\fancypagestyle{plain}{%
    \fancyhf{}
    \fancyhead[RO]{\small Software para el Análisis de Datos}
    \fancyhead[LE]{\small Dra.\ Marta Barea Sepúlveda}
    \fancyfoot[C]{\thepage}
    \renewcommand{\headrulewidth}{0.4pt}
    \renewcommand{\footrulewidth}{0pt}
}

\AtBeginDocument{
    \hypersetup{
        colorlinks=true,
        linkcolor=uocblue,
        urlcolor=uocblue,
        citecolor=uocblue,
        pdfborder={0 0 0}
    }
}

% Redefinir \maketitle para portada personalizada
\makeatletter
\renewcommand{\maketitle}{%
    \thispagestyle{empty}%
    \begin{tikzpicture}[remember picture,overlay]
        % Logo UB
        \node[anchor=north west, xshift=2.5cm, yshift=-1.5cm] at (current page.north west) {%
            \includegraphics[width=0.2\textwidth]{../assets/images/ub_logo.png}};
        % Logo UOC
        \node[anchor=north east, xshift=-2.5cm, yshift=-1.5cm] at (current page.north east) {%
            \includegraphics[width=0.2\textwidth]{../assets/images/uoc_logo.png}};
        % Autora y fecha
        \node[anchor=south west, xshift=2.5cm, yshift=2cm, text width=10cm, align=left] at (current page.south west) {%
            \normalsize\textbf{Autora:} \@author\\[3pt]%
            \textbf{Fecha:} \@date};
    \end{tikzpicture}%

    \vspace*{5cm}%

    \begin{center}
        {\normalsize\textbf{MÁSTER EN BIOINFORMÁTICA Y BIOESTADÍSTICA}}\\[1cm]
        {\Large\textbf{PEC 4}}\\[0.5cm]

        \rule{0.8\textwidth}{1.2pt}\\[0.6cm]

        \begin{minipage}{0.8\textwidth}
            \centering
            \fontsize{24}{29}\selectfont\bfseries \@title\par
        \end{minipage}

        \vspace{0.6cm}

        \rule{0.8\textwidth}{1.2pt}

        \vspace{1cm}

        {\Large Software para el Análisis de Datos}
    \end{center}

    \vfill
    \clearpage

    % Suprimir numeración en páginas de la tabla de contenidos
    \pagestyle{empty}%
}
\makeatother